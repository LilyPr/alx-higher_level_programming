#!/usr/bin/node
/* Prints 3 lines using an array of str and a loop */
const myArr = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let i = 0; i < myArr.length; i++) {
  console.log(myArr[i]);
}
